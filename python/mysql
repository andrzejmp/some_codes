
Connecting:

Use these settings in your web applications.
Database host address:
andrzejgmx.mysql.pythonanywhere-services.com
Username:
andrzejgmx
Your databases:

Click a database's name to start a MySQL console logged in to it.
Start a console on:
andrzejgmx$default
Create a database

Your database names always start with your username + "$". There's no need to type that prefix in below, though: PythonAnywhere will automatically add it.

create table students (
    id INT NOT NULL AUTO_INCREMENT,    
    NAZWISKO VARCHAR(40) NOT NULL, 
    IMIE VARCHAR(20) NOT NULL,    
    PRYWATNY VARCHAR(60) NOT NULL,
    SLUZBOWY VARCHAR(60) NOT NULL,    
    INDEKS VARCHAR(10) NOT NULL,      
    PRG_KOD VARCHAR(20) NOT NULL,      
    PRIMARY KEY (id));
